{
  "id": "chapter-28",
  "number": 28,
  "title": "Template-driven Forms",
  "summary": "Build forms right in the template with ngModel — great for simple forms.",
  "kicker": "Chapter 28",
  "group": "Angular",
  "heading": "Template-driven Forms",
  "sections": [
    {
      "id": "two-kinds",
      "title": "Two ways to build forms",
      "blocks": [
        {
          "type": "text",
          "text": "Angular gives you two styles of forms. They do the same job — collect and validate user input — but put the logic in different places."
        },
        {
          "type": "stack",
          "layers": [
            {
              "title": "Template-driven",
              "detail": "Form logic lives in the template with ngModel. Simple and quick — this chapter."
            },
            {
              "title": "Reactive",
              "detail": "Form model lives in the TypeScript class. More powerful for big or dynamic forms — next chapter."
            }
          ]
        },
        {
          "type": "note",
          "text": "Reach for template-driven forms for small things like a login or contact form. Reach for reactive forms when the form is large, dynamic, or needs serious validation and testing."
        },
        {
          "type": "link",
          "url": "https://angular.dev/guide/forms/template-driven-forms",
          "label": "More on angular.dev"
        }
      ]
    },
    {
      "id": "setup",
      "title": "Setup",
      "blocks": [
        {
          "type": "text",
          "text": "Template-driven forms come from <code>FormsModule</code>. Add it to the component's <code>imports</code>."
        },
        {
          "type": "code",
          "label": "component",
          "code": "import { Component } from '@angular/core';\nimport { FormsModule } from '@angular/forms';\n\n@Component({\n  selector: 'app-signup',\n  imports: [FormsModule],\n  templateUrl: './signup.html'\n})\nexport class Signup {\n  model = { name: '', email: '' };\n}"
        }
      ]
    },
    {
      "id": "ngmodel",
      "title": "Binding inputs with ngModel",
      "blocks": [
        {
          "type": "text",
          "text": "Each input uses <code>[(ngModel)]</code> for two-way binding and a <code>name</code> attribute so the form can track it. Wrap the inputs in a <code>&lt;form&gt;</code> and handle <code>(ngSubmit)</code>."
        },
        {
          "type": "code",
          "label": "signup.html",
          "code": "<form (ngSubmit)=\"submit()\">\n  <input name=\"name\" [(ngModel)]=\"model.name\" placeholder=\"Name\">\n  <input name=\"email\" [(ngModel)]=\"model.email\" placeholder=\"Email\">\n  <button type=\"submit\">Sign up</button>\n</form>"
        },
        {
          "type": "code",
          "label": "component",
          "code": "submit() {\n  console.log(this.model); // { name: '...', email: '...' }\n}"
        }
      ]
    },
    {
      "id": "validation",
      "title": "Validation",
      "blocks": [
        {
          "type": "text",
          "text": "Add normal HTML validators like <code>required</code> and <code>minlength</code>. Give the input a template reference (<code>#name=\"ngModel\"</code>) to read its state and show messages."
        },
        {
          "type": "code",
          "label": "signup.html",
          "code": "<input\n  name=\"name\"\n  [(ngModel)]=\"model.name\"\n  required\n  minlength=\"3\"\n  #name=\"ngModel\">\n\n@if (name.invalid && name.touched) {\n  <small>Name must be at least 3 characters.</small>\n}"
        },
        {
          "type": "note",
          "text": "Useful states on a control: <code>valid</code> / <code>invalid</code>, <code>touched</code> (the user visited it), and <code>dirty</code> (the value changed)."
        }
      ]
    },
    {
      "id": "form-state",
      "title": "Using the whole form's state",
      "blocks": [
        {
          "type": "text",
          "text": "Give the form a reference with <code>#form=\"ngForm\"</code> to check if the entire form is valid — handy for disabling the submit button."
        },
        {
          "type": "code",
          "label": "signup.html",
          "code": "<form #form=\"ngForm\" (ngSubmit)=\"submit()\">\n  <input name=\"name\" [(ngModel)]=\"model.name\" required>\n  <button type=\"submit\" [disabled]=\"form.invalid\">\n    Sign up\n  </button>\n</form>"
        },
        {
          "type": "text",
          "text": "You can also pass that <code>#form</code> reference straight into your method — <code>(ngSubmit)=\"submit(form)\"</code> — so the component reads the form's value and validity:"
        },
        {
          "type": "code",
          "label": "signup.ts",
          "code": "import { NgForm } from '@angular/forms';\n\nsubmit(form: NgForm) {\n  if (form.invalid) return;\n  console.log(form.value);   // { name: '...' }\n  console.log(form.valid);   // true\n  form.resetForm();          // clear all fields\n}"
        },
        {
          "type": "text",
          "text": "Or grab it from the class without passing it, using <code>viewChild</code>:"
        },
        {
          "type": "code",
          "label": "signup.ts",
          "code": "import { viewChild } from '@angular/core';\nimport { NgForm } from '@angular/forms';\n\nform = viewChild.required<NgForm>('form');\n\nsave() {\n  const f = this.form();\n  console.log(f.value, f.valid);\n}"
        }
      ]
    },
    {
      "id": "td-nested",
      "title": "Nested groups",
      "collapsed": true,
      "blocks": [
        { "type": "text", "text": "Group related fields with <code>ngModelGroup</code>. The grouped controls become a nested object inside the form value." },
        { "type": "code", "label": "template", "code": "<form #f=\"ngForm\">\n  <input name=\"name\" ngModel>\n\n  <div ngModelGroup=\"address\">\n    <input name=\"street\" ngModel>\n    <input name=\"city\" ngModel>\n  </div>\n</form>" },
        { "type": "code", "label": "f.value", "code": "{\n  name: '...',\n  address: { street: '...', city: '...' }\n}" }
      ]
    },
    {
      "id": "td-split",
      "title": "Splitting a form across components",
      "collapsed": true,
      "blocks": [
        { "type": "text", "text": "To move part of a form into a child component, let the child reuse the parent's <code>NgForm</code> through <code>viewProviders</code>. Then the child's <code>ngModel</code> controls register with the parent form automatically." },
        { "type": "code", "label": "address.ts (child)", "code": "import { Component } from '@angular/core';\nimport { ControlContainer, NgForm, FormsModule } from '@angular/forms';\n\n@Component({\n  selector: 'app-address',\n  imports: [FormsModule],\n  viewProviders: [\n    { provide: ControlContainer, useExisting: NgForm }\n  ],\n  template: `\n    <input name=\"street\" [(ngModel)]=\"model.street\">\n    <input name=\"city\" [(ngModel)]=\"model.city\">`\n})\nexport class Address {\n  model = { street: '', city: '' };\n}" },
        { "type": "note", "text": "Without that <code>viewProviders</code> line, the child's inputs wouldn't join the parent form." }
      ]
    },
    {
      "id": "td-array",
      "title": "A list of fields (form array)",
      "collapsed": true,
      "blocks": [
        { "type": "text", "text": "Template-driven forms have no <code>FormArray</code>. For a dynamic list, loop over an array in your model and give each input a unique <code>name</code>." },
        { "type": "code", "label": "component", "code": "phones = [''];\n\naddPhone() { this.phones.push(''); }\nremovePhone(i: number) { this.phones.splice(i, 1); }" },
        { "type": "code", "label": "template", "code": "@for (phone of phones; track i; let i = $index) {\n  <input [name]=\"'phone' + i\" [(ngModel)]=\"phones[i]\">\n  <button type=\"button\" (click)=\"removePhone(i)\">✕</button>\n}\n<button type=\"button\" (click)=\"addPhone()\">Add phone</button>" },
        { "type": "note", "text": "This works but is manual. For dynamic lists, reactive forms' <code>FormArray</code> is cleaner — see the Reactive Forms chapter." }
      ]
    },
    {
      "id": "td-recap",
      "title": "Recap",
      "blocks": [
        {
          "type": "list",
          "items": [
            "Template-driven forms keep the logic in the template; import <code>FormsModule</code>.",
            "Bind inputs with <code>[(ngModel)]</code> and give each a <code>name</code>.",
            "Handle submit with <code>(ngSubmit)</code>.",
            "Validate with <code>required</code>, <code>minlength</code>, etc.; read state via <code>#ref=\"ngModel\"</code>.",
            "Use <code>#form=\"ngForm\"</code> for the whole form's validity.",
            "Best for small, simple forms."
          ]
        }
      ]
    }
  ]
}
